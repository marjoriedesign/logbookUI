# PROGRESS.md — journal d'avancement

Factuel uniquement. Mis à jour à la fin de chaque session (cf. CLAUDE.md).

## Fait et validé

- Scaffold initial : MUI v9 + RemixIcon, Storybook, pipeline de tokens (Tokens Studio → `tokens:build` → thème).
- Charte réelle Logbook (palette teal/corail, police Zain) intégrée dans `tokens/core.json` et `tokens/semantic.json`.
- `semantic.json` référence désormais `core.json` par pointeurs (`"value": "{color.cyan.900}"`) plutôt que par valeurs figées, sur toutes les couleurs sauf `common.white`/`common.black` (primitives dédiées, aucune rampe n'a de blanc/noir pur).
- Rampes `yellow`/`orange` remplacées par les teintes moins vibrantes fournies par Marjorie.
- Échelle de spacing étendue avec deux crans fins en bas d'échelle : `2xs` (6px), `3xs` (4px).
- Audit AA (WCAG) mené sur Button et Chip : contrastes corrigés (`contained` success/error → palier `.dark`, `filled` Chip success/error/warning → `.dark`), nouveau palier Chip `subtle` (fond pastel + texte sur `text.secondary`) pour les statuts discrets.
- Contraste du fallback "+N" d'`AvatarGroup` corrigé (1.87:1 → 8.42:1).
- Typography : `h4`–`h6`, `subtitle1`/`subtitle2`, `caption`, `overline` branchés sur leurs tokens (définis mais inutilisés jusqu'ici).
- Composants MUI thémés (`src/theme/components/`) : Button, IconButton, Switch, Badge, Avatar, AvatarGroup, Alert, Checkbox, Select, Chip, Table, TextField, Card, AppBar, Toolbar, FormControlLabel.
- Wrappers Logbook sans équivalent MUI natif (`src/components/`) : `LogbookIconButton`, `LogbookNavbar`, `LogbookListenProgress`.
- Composant produit `CorrectionsTable` (catégorie Storybook Logbook, distincte de Components) : suivi des corrections, switch de remise, chips/boutons d'état, icônes ballpen/bard, écoute, note, réaction ; header responsive robuste (colonnes retirées du DOM sous 900px, pas juste masquées) ; tri sur la colonne Élève.
- Storybook : un composant = une seule page ; interlignage 120% harmonisé sur toutes les pages ; build/déploiement Storybook et Chromatic réactivés.
- `tsconfig` strict activé.
- Repo Git poussé sur GitHub (`origin/main` = `github.com/marjoriedesign/logbookUI`, à jour avec le local).
- Champs `_states` (selected/focus/focusVisible/outlinedBorder par couleur, `action.active/selected/focus/disabled/disabledBackground`, `common.black_states`/`white_states`) nettoyés dans `tokens/semantic.json` : remplacés par les vraies valeurs par défaut de MUI (superpositions alpha sur `main`/noir/blanc aux opacités officielles MUI 4/8/12/50%, encodées en hex 8 chiffres) au lieu des couleurs pleines placeholder jamais nettoyées de l'export Tokens Studio initial. `action.active/selected/focus/disabled/disabledBackground` câblé explicitement dans `palette.ts` (purement déclaratif, aucun changement de rendu — `action.hover` et `success/error/warning/info._states.hover`, seuls champs déjà consommés par `Button.ts`, restent inchangés). Documenté dans le README, section "Autres champs `_states`".
- `typography.button.small/medium/large` : converti en vrai token composite (fontSize/fontWeight/lineHeight/letterSpacing/textCase), verrouillant ce que `Button.ts` rendait déjà (fontWeight 700, lineHeight 120%, letterSpacing 0 — MUI n'applique son calcul de letterSpacing par défaut que si `fontFamily` reste Roboto, jamais le cas ici puisque le thème force Zain). `Button.ts`/`Chip.ts` mis à jour pour lire `.fontSize` sur l'objet composite au lieu d'un nombre brut. Aucun changement de rendu.
- Page Foundations/Typography : passée d'une liste de 6 variantes codées en dur à un parcours générique des 34 styles de `tokens/semantic.json`, tout niveau d'imbrication confondu.
- Storybook : panneau Controls désactivé sur les 20 pages sans props pilotables (5 Foundations + 15 démos statiques), gardé sur les 6 pages avec de vraies `args`/`argTypes` (Button, Chip, IconButton, Switch, TextField, Logbook/ListenProgress).
- Storybook déployé sur GitHub Pages (`npm run deploy-storybook`, branche `gh-pages`) : reflète l'état courant.
- Doc "À savoir" sur les pages Components (pas Logbook, qui assemble des composants déjà documentés individuellement) : décisions/exceptions importantes par composant (corrections AA, overrides de comportement natif MUI, valeurs figées justifiées, incohérences connues comme `primary.dark` vert sur IconButton). D'abord testée avec un détail complet des tokens par composant, simplifiée sur demande de Marjorie : encadré beige (`secondary.subtle.background`) avec icône ampoule, affiché après la démo plutôt qu'avant.
- Nouvelle page `Foundations/Tokens` (après Visuals) : liste les tokens de couleur façon Figma (icône palette + nom, swatch + référence "famille / cran", en-têtes "groupe / sous-groupe"). **Core** lit `tokens/core.json` brut et n'affiche que les familles/crans réellement référencés par Theme (9 familles sur les 19 complètes du nuancier Material importé). **Theme** parcourt `tokens/semantic.json` (pas `designTokens/generated`, qui a résolu chaque référence en hex et perdu l'alias d'origine) pour retrouver la chaîne "cyan / 900", et fusionne automatiquement les champs qui résolvent à la même valeur (ex. les 24 `paper-elevation-N` à blanc).
- `Chip.ts` relié à `typography.chip.label` (13px/400/18px) au lieu de réutiliser `typography.button.small` — le texte des chips passe de bold à regular (changement visuel assumé).
- Tokens `strong.background` ajoutés pour error/warning (`{color.orange.800}`) et info (`{color.teal.700}`, remplace l'ancien `{color.deeppurple.700}` hors palette) : le fond "Strong" de Chip ne lit plus les primitives `core.json` directement.
- Tokens `subtle.background` ajoutés pour success (`{color.green.100}`) et info (`{color.teal.100}`) ; celui de warning corrigé de `{color.orange.100}` (qui collisionnait avec error) à `{color.yellow.200}` — les 5 fonds "Subtle" de Chip viennent maintenant uniquement des tokens, plus aucune couleur posée en dur dans les stories.
- `Table` (`MuiTable`) a désormais un fond blanc explicite (`color.background['paper-elevation-0']`) au lieu de dépendre par transparence du fond de page (`background.default`) — même rendu, composant autonome.
- Header de `MuiTableCell` (toutes les tables du DS) : texte passé de `text.primary` à `text.secondary`, et de la variante `head` native de MUI (fontWeight medium, lineHeight 24px) au token `typography.subtitle1` (`body1` essayé d'abord, jugé trop imposant par Marjorie).
- `CorrectionsTable`, colonne "Élève" : `TableSortLabel` remplacé par un `Button` (`color="secondary"`, `size="small"`, finalement `variant="text"` après essais `contained`/`outlined`) + icône `RiArrowDownLine` pivotant selon le sens de tri — comportement de tri et `aria-sort` (porté par `TableCell`) inchangés.
- `LogbookListenProgress` : override `fontWeight: semibold` retiré du pourcentage affiché, désormais identique en style (`body2` + `text.secondary`) à la colonne Note de `CorrectionsTable`, incohérence non documentée à l'origine.
- `Card` (`MuiCard`) : `boxShadow` forcé à `'none'` (au lieu du token `shadow.sm`) — plus aucune ombre portée sur les Card du DS, changement global. Note "À savoir" de `Components/Card.stories.tsx` corrigée en conséquence (elle décrivait l'ancienne ombre).
- Nouveau composant `LogbookStatCard` (`src/components/LogbookStatCard.tsx`, story `Logbook/StatCard`) : carte stat (icône + libellé + valeur + suffixe optionnel), construite sur `Card` sans aucun override de fond/bordure/rayon (le style par défaut de `Card` correspond déjà à la maquette). Props : `icon`, `iconAlt`, `label`, `value`, `suffix`. Réglages actuels : icône 48×48 (réutilise `Certification.svg`, déjà dans `src/assets/illustrations`), libellé en `h5` (`body1` essayé d'abord), valeur en `h4`, suffixe en `subtitle2`, largeur fixe 128px, padding 8px (`spacing.xs`).
- Nouveau thème `MuiTabs`/`MuiTab` (`src/theme/components/Tabs.ts`, story `Components/Tabs`) : onglets à coins arrondis en haut (`borderRadius.lg`), contour `divider`, fond `background.paper` uniquement sur l'onglet actif, indicator natif de MUI élargi à 4px (aucun token dédié à une épaisseur de trait dans le DS, valeur figée assumée) en `primary.main`. Couleur du libellé/icône identique actif/inactif — déroge à l'atténuation native de MUI sur les onglets inactifs, choix assumé pour coller à la maquette de Marjorie, documenté dans le "À savoir" de la story.
- Point d'entrée public (`src/index.ts` + champs `main`/`types` dans `package.json`) : le repo n'exposait jusqu'ici aucune API consommable de l'extérieur. Ajouté pour permettre l'installation en dépendance git par un projet client (`logbook-dashboard`, dashboard prof de suivi des corrections). Réexporte `LogbookThemeProvider`, `logbookTheme`, les composants `Logbook*` et les icônes RemixIcon utilisées. Toute nouvelle brique destinée à être consommée à l'extérieur doit passer par ce fichier.
- `CorrectionsTable` extrait en vrai composant exporté (`src/components/CorrectionsTable.tsx`) : n'existait auparavant que comme code de démo inline dans sa story, pas consommable ailleurs. API contrôlée par props (`rows` + callbacks `onToggleSubmitted`/`onOpenStudent`/`onOpenActions`) ; le tri par nom reste un état interne purement présentationnel. Réexporté dans le barrel public.
- **Décision tranchée par Marjorie** — colonne État de `CorrectionsTable` : uniquement des Chips, jamais de Button. Raison : la ligne entière est cliquable (ouvre un side panel), un bouton imbriqué dans un élément cliquable poserait un problème d'imbrication de contrôles interactifs. "À corriger"/"À analyser" sont donc passés de `Button` (`contained`/`primary`) à `Chip` (`variant="filled"`, `color="primary"`, icônes ballpen/bard via la prop `icon` de Chip — premier usage de cette prop dans le DS, jusqu'ici jamais stylée/auditée) : rendu quasi identique (même teal, mêmes coins 8px) mais non-interactif. Conséquence directe : `onCorrect`/`onAnalyze` n'avaient plus aucun déclencheur → retirés de l'API publique (vérifié que `logbook-dashboard`, seul consommateur connu, ne les utilisait pas).
- `CorrectionsTable`, ligne entière cliquable : `TableRow` porte `onClick={() => onOpenStudent?.(row)}` (+ curseur pointer conditionnel). Le Switch et le bouton "..." Actions stoppent la propagation (`event.stopPropagation()`) pour ne pas déclencher l'ouverture du side panel par accident ; la flèche `>` a perdu son `onClick` propre (redondant, remonte désormais au `TableRow`).
- `MuiTableCell`, nouveau slot `body` : `paddingBlock` réduit à 8px (`spacing.xs`) au lieu des 16px par défaut MUI — scopé aux lignes du corps uniquement (le header garde son padding normal), padding horizontal inchangé.
- **Rampe `purple` remplacée** (`tokens/core.json`) : les crans `50`–`900` passent d'un placeholder Material générique à une échelle bleu-violet fournie par Marjorie (`50` = `#eaebf4` … `900` = `#32297e`) ; les accents `a100`–`a700` ne changent pas (déjà identiques à la maquette fournie). **Palette `info` reliée à `purple`** (`tokens/semantic.json`) au lieu de `teal` : `main`→`purple.100`, `dark`→`purple.700`, `light`→`purple.50`, `contrastText` inchangé (`grey.800`), `strong.background`→`purple.700`, `subtle.background`→`purple.100`. Les 4 champs `_states` (hover/selected/focusvisible/outlinedborder) recalculés en alpha sur `purple.100` (8/8/12/50%, même encodage hex 8 chiffres que les autres couleurs) — `hover` n'est plus une teinte pleine (`teal.800` avant) mais un alpha, aligné sur `selected`. Contrastes revérifiés avec les nouvelles valeurs : `info.contrastText` sur `info.main` 6.12:1, `info.dark` sur blanc 7.94:1, texte blanc sur `info.dark` (hover de Button/Chip contained) 7.94:1 — tous conformes AA. Impact : partout où `color="info"` est utilisé (Chip, Button, Alert, Checkbox, IconButton, "Consultée" de `CorrectionsTable`) passe du teal au purple. Commentaires d'audit AA obsolètes (citant les anciens chiffres teal) corrigés dans `Alert.ts`/`Alert.stories.tsx`, `Button.ts`, `Chip.ts`/`Chip.stories.tsx`.
- Nouveau composant `LogbookSidePanelHeader` (`src/components/LogbookSidePanelHeader.tsx`, story `Logbook/SidePanelHeader`) : header de side panel (avatar 48×48 + nom en `h4`/`text.secondary` à gauche, bouton fermer à droite), 480px de large, fond `background.default`, padding 16px, contour bas 1px `divider`. Props : `studentName`, `avatarSrc`, `avatarAlt`, `onClose`.
- Nouveau composant `LogbookVoiceRecorder` (`src/components/LogbookVoiceRecorder.tsx`, story `Logbook/VoiceRecorder`) : bouton d'enregistrement vocal du prof sur une correction, 2 états sur 3+ prévus (`idle`/`recording`, les autres à venir). Container `Card` (comme `StatCard`, sans override), 448px de large, padding vertical 16px. Bouton `Fab color="primary"` 64×64 (`aspect-ratio: 1/1`, sans ombre), icône mic/pause selon l'état. En enregistrement : titre `h5`, consigne clavier + minuteur en `subtitle2`/`text.secondary`, vague animée (20 barres 2px, `primary.dark`, keyframes CSS). Composant purement présentationnel (`status`, `elapsedSeconds`, `onToggle`) ; le raccourci espace est démontré dans la story, pas dans le composant. Au survol du bouton, MUI bascule vers `primary.dark` (#4ad09a, vert) — même incohérence connue déjà documentée ailleurs, pas un bug introduit ici.
- **`LogbookIconButton` restructuré pour matcher la bibliothèque Figma de Marjorie** : celle-ci n'a pas de notion de `variant` séparée (contrairement au code jusqu'ici, qui avait `variant: 'filled' | 'outlined'` + `color`) — chaque `color` y a un rendu fixe. Prop `variant` retirée entièrement ; nouveau mapping (délibérément inversé par rapport aux tokens de palette du même nom, propre à ce composant) : `primary` → contour beige (`secondary.main`, anciennement "outlined secondary") ; `secondary` → fond plein teal + icône blanche (`primary.main`/`contrastText`, anciennement "filled primary") ; `success`/`warning`/`error`/`info` inchangés (fond plein, déjà audités AA) ; `default`/`inherit` inchangés (icône seule, comportement natif MUI). Tous les appels existants dans ce repo mis à jour pour préserver leur rendu (`variant="outlined" color="secondary"` → `color="primary"`) : `CorrectionsTable`, `LogbookSidePanelHeader`, `LogbookNavbar` (3 boutons), `Badge.stories.tsx` (3 démos). Type `LogbookIconButtonVariant` (obsolète) retiré de `src/index.ts`.

- `LogbookSidePanelHeader` et `LogbookVoiceRecorder` réexportés dans le barrel public (`src/index.ts`) : ajoutés dans un commit précédent mais oubliés lors de leur création, donc inutilisables depuis `logbook-dashboard` jusqu'ici.
- `LogbookSidePanelHeader` : fond passé de `background.default` à `color.background['paper-elevation-0']` (même pattern que `MuiTable`, non câblé sur `palette.ts`) pour un aplat blanc identique au reste du panel plutôt qu'un contraste involontaire avec le fond de page.
- Nouveau thème `MuiDrawer` (`src/theme/components/Drawer.ts`) : fond `paper-elevation-0` au lieu de l'héritage par défaut de `background.paper` (`paper-elevation-1`, grey.50) qui créait une coupure visible avec `LogbookSidePanelHeader`. S'applique à tout `Drawer` MUI consommé tel quel par les projets clients.
- `LogbookSidePanelHeader`/`LogbookVoiceRecorder` : largeur fixe (480px/448px) remplacée par `{ xs: '100%', sm: <valeur fixe> }` — débordait hors écran sur mobile (le composant ne connaissait pas la largeur de son conteneur, `logbook-dashboard` doit répliquer le même point de rupture sur le `Drawer` qui l'accueille).
- `LogbookNavbar` : `position="static"` → `position="sticky"` sur l'`AppBar` — reste fixe en haut de la page au scroll (comportement demandé pour l'écran de suivi de classe, où le tableau peut dépasser la hauteur de la fenêtre).
- Nouveau thème `MuiLinearProgress` (`src/theme/components/LinearProgress.ts`) : hauteur fixée à 8px et coins arrondis à `borderRadius.md` (8px) sur `root` et `bar`, au lieu des 4px carrés par défaut de MUI — demandé par Marjorie, s'applique à tout `LinearProgress` du DS.
- Nouveau composant `LogbookVoicePlayer` (`src/components/LogbookVoicePlayer.tsx`, story `Logbook/VoicePlayer`) : lecteur audio pour le prof qui réécoute son propre vocal (pendant qu'il corrige), pendant que `LogbookVoiceRecorder` sert à l'enregistrer. Container `Card` (fond/bordure beige hérités du défaut, comme `LogbookVoiceRecorder`), 448px de large. Temps courant/durée en `subtitle2`, `LinearProgress` juste en dessous. Bandeau de contrôles séparé : `Box` bordée `divider`, fond `paper-elevation-0` (même pattern que `LogbookSidePanelHeader`), `borderRadius.lg` — contient bouton vitesse (`Button variant="outlined" color="secondary"`, affiche `x{valeur}`), boutons ±30s et transcription (`LogbookIconButton color="primary"`, icônes `RiReplay30Line`/`RiForward30Line`/`RiAiGenerateText`), et bouton play/pause central `Fab color="primary"` 64×64 (même gabarit que le bouton mic de `LogbookVoiceRecorder`), icône `RiPlayFill` à l'arrêt (`RiPauseLine` en lecture). Props : `isPlaying`, `currentSeconds`, `durationSeconds`, `playbackRate`, callbacks `onPlayPause`/`onSkipBack`/`onSkipForward`/`onPlaybackRateClick`/`onTranscriptClick` — composant purement présentationnel, aucune lecture audio réelle en interne. Vérifié en Playwright (styles calculés) : `LinearProgress` 8px/8px de rayon conformes, `Fab` 64×64, aucune violation axe-core (wcag2a/2aa). Réexporté dans le barrel public (`src/index.ts`).
- `RiPlayFill` ajouté au barrel d'icônes (`src/icons/index.ts`) : exception explicite au principe "outline only" (même type d'exception que `RiMoreFill`), demandée par Marjorie pour le bouton play de `LogbookVoicePlayer` précisément — `RiPauseLine` (état lecture du même bouton) reste en outline, non concerné par la demande.
- Story `Logbook/VoicePlayer` : minuteur de démo passé de `setInterval`/+1s à `requestAnimationFrame` + temps réel écoulé (fractions de seconde) — les incréments entiers d'une seconde produisaient des saccades visibles sur la `LinearProgress` (transition CSS de MUI, 0.4s, trop courte pour lisser un pas aussi large). Changement scopé à la story de démo, `LogbookVoicePlayer` reste inchangé (reflète simplement la prop `currentSeconds` reçue).
- `LogbookVoicePlayer`, bandeau de contrôles : fond `paper-elevation-0` retiré (demandé par Marjorie) — devient transparent, seul le contour `divider` reste visible, le beige de la `Card` parente transparaît au travers.
- Nouveau composant `LogbookSidePanelFooter` (`src/components/LogbookSidePanelFooter.tsx`, story `Logbook/SidePanelFooter`) : bas de side panel portant les boutons d'action principaux, pendant du haut de panel `LogbookSidePanelHeader`. 1 ou 2 boutons (`primaryAction`/`secondaryAction`, chacun `label`/`onClick`/`variant`/`color`/`disabled`/`startIcon`/`endIcon`) ; si `secondaryAction` est fourni il s'affiche à gauche, en `color="warning"` par défaut (surchargeable), `primaryAction` reste au variant/couleur par défaut du thème (`contained`/`primary`) sauf override. 480px de large (aligné sur `LogbookSidePanelHeader`), fond blanc (`paper-elevation-0`), contour uniquement en haut (`borderTop` 1px `divider`), padding 16px, 8px entre les deux boutons (`gap: 1`). Largeur des boutons : `flex: '1 1 auto'` sur chacun — par défaut ils se partagent la largeur à parts égales, mais si le libellé de l'un est plus long, il part d'une base de contenu plus large et reste plus large à l'arrivée (pas de partage forcé 50/50, pas de troncature). Boutons en `size="large"` (fixe, non exposé en prop). Vérifié en Playwright (largeurs/hauteurs mesurées sur les 4 cas de la story) + axe-core (0 violation wcag2a/2aa). Réexporté dans le barrel public.
- Storybook GitHub Pages redéployé (`npm run deploy-storybook`) pour inclure `LogbookVoicePlayer`/`LogbookSidePanelFooter` — le déploiement est une étape manuelle séparée du push sur `main` (aucun workflow CI ne le déclenche automatiquement), à relancer après toute session qui ajoute/modifie un composant ou une story.
- Nouveau composant `LogbookCommentField` (`src/components/LogbookCommentField.tsx`, story `Logbook/CommentField`) : `TextField` MUI légèrement modifié (rayon `borderRadius.lg`/12px au lieu de `radius.sm` global, pas de label flottant dans le contour) pour les commentaires du prof sur une copie une fois l'audio analysé (ex. "Points positifs" / "Points à améliorer"), dans le side panel. Le `TextField` MUI standard (`theme/components/TextField.ts`) garde son rayon (`radius.sm`) et son label flottant natif — seul le rayon/l'absence de label sont propres à `LogbookCommentField`, le reste (bordure beige, typo du texte saisi...) vient du thème global partagé. Icône + libellé (`icon`/`label`, ex. `RiThumbUpLine` en `designTokens.color.purple[500]`) affichés au-dessus en Typography `h4`, en dehors du contour du champ. Le libellé n'étant pas le `<label>` MUI natif, `slotProps.htmlInput` porte un `aria-label` par défaut égal à `label` (sinon le champ n'a aucun nom accessible — détecté par une violation axe-core `label`, corrigée). `multiline`/`minRows` par défaut (`true`/2), le reste des props `TextField` (`value`, `onChange`, `placeholder`...) passe tel quel. Vérifié en Playwright (rayon 12px, contour beige hérité du thème, `aria-label` présent) + axe-core (0 violation wcag2a/2aa). Réexporté dans le barrel public.
- `MuiOutlinedInput` (`theme/components/TextField.ts`), slot `input` : typo du texte saisi branchée sur `typography.textField.text` (16px/400/24, jusqu'ici défini dans les tokens mais jamais consommé) au lieu du `body1` du thème hérité par défaut (20px) — s'applique à tous les `TextField` outlined du DS, y compris `LogbookCommentField`. Vérifié en Playwright (styles calculés : 16px/400/24px/Zain) sur `Components/TextField` et `Logbook/CommentField`.
- `LogbookCommentField`, libellé : `color="text.secondary"` corrigé en `color="textSecondary"` — voir bug plus large en "Dette technique" ci-dessous, repéré à cette occasion.
- **Dette technique `color="text.*"` (point) résolue partout** : les 25 occurrences restantes corrigées en camelCase (`"textSecondary"`/`"textPrimary"`/`"textDisabled"`), 16 fichiers — `App.tsx`, `LogbookVoiceRecorder.tsx` (3), `LogbookSidePanelHeader.tsx`, `LogbookListenProgress.tsx`, `LogbookStatCard.tsx`, `LogbookVoicePlayer.tsx`, `CorrectionsTable.tsx` (3), `stories/PageLayout.tsx`, et les stories `Foundations/Typography` (2), `Foundations/Spacing`, `Foundations/Colors`, `Foundations/Tokens` (4), `Components/Card` (2), `Components/Avatar`, `Components/LinearProgress`, `Components/Breadcrumbs`. Remplacement mécanique par script (pas un remplacement en masse aveugle : diff relu fichier par fichier, `tsc -b --force` clean, couleurs recalculées vérifiées en live sur 5 stories représentatives — `text.secondary`/`#47453f`/`rgb(71,69,63)` et `text.primary`/`#000000`/`rgb(0,0,0)` conformes partout), puis scan axe-core sur 16 stories couvrant tous les fichiers touchés. A fait remonter un vrai défaut de contraste pré-existant sur `text.disabled` (2.58:1 sur fond blanc, sous l'AA), utilisé comme préfixe de libellé atténué dans `SubgroupHeading` (`Foundations/Tokens.stories.tsx:192`, "grey /" avant le nom du cran) — pas un contrôle de formulaire désactivé, donc pas couvert par la dispense WCAG habituelle sur ce token.
- Nouveau composant `LogbookDropzone` (`src/components/LogbookDropzone.tsx`, story `Logbook/Dropzone`) : zone de dépôt de fichier (sujet, corrigé, grille d'évaluation...). Container `Card` (même fond/bordure beige hérités du défaut que `LogbookVoiceRecorder`), contour repassé en pointillé (`borderStyle: 'dashed'`, propre à ce composant — `theme/components/Card.ts` inchangé), padding 16px sur les 4 côtés, largeur 100% (contrairement à `VoiceRecorder`/`VoicePlayer` qui ont une largeur fixe, celui-ci s'adapte à son conteneur comme un champ de formulaire). Icône `RiUploadCloudLine` (`purple[500]`, même token que l'icône de `LogbookCommentField`) + titre `h5`/`textPrimary` + sous-texte `subtitle2`/`textSecondary`, tous personnalisables (`label`, `helperText`). Interactif : toute la carte est cliquable (`role="button"`, `tabIndex`, `Entrée`/`Espace` au clavier) et ouvre un `<input type="file">` caché ; drag-and-drop HTML5 natif également géré (`onDragOver`/`onDrop`). Props `accept`, `multiple` (true par défaut), `disabled` (opacité réduite, `aria-disabled`, plus de gestionnaires actifs), `onFilesSelected`. Survol : fond `secondary.subtle.background` (`#f3f0e9`) + contour `primary.main` (teal) — `action.hover` (`#fdfaf3`) écarté car identique au fond de repos de la Card (invisible), désactivé sur l'état `disabled`. Vérifié en Playwright (fond `#fdfaf3`, bordure pointillée 1px beige, padding 16px, `tabIndex=0`, survol mesuré avant/après avec `hover()`, aucun changement sur `disabled`) + axe-core (0 violation wcag2a/2aa). Réexporté dans le barrel public.
- **Décision tranchée par Marjorie** — contraste réduit de `text.disabled` accepté tel quel pour ce rôle : la sémantique "disabled" du nom du token justifie de ne pas l'astreindre au même seuil AA qu'un texte normal, même si son usage ici (préfixe "grey /") n'est pas un contrôle de formulaire au sens strict WCAG. Pas de changement de token ni de nouvel arbitrage nécessaire pour l'instant.
- `designTokens` réexporté dans le barrel public (`src/index.ts`, en plus de `logbookTheme`) : `logbook-dashboard` en avait besoin pour reproduire la couleur d'icône `purple[500]` de la story `Logbook/CommentField` (`RiThumbUpLine`) sans import profond dans son `App.tsx`.
- **Découverte process** : `npx tsc --noEmit` ne vérifie rien dans ce repo (tsconfig racine "solution style", `"files": []` + `references`, ignoré sans le flag `-b`) — a laissé passer une vraie erreur de type (`inputProps` retiré de `TextFieldProps` en MUI v9, remplacé par `slotProps.htmlInput`) pendant une partie de la session avant d'être repérée via `npx tsc -b --force` (la commande déjà documentée comme rituel de vérification). Tout le travail de la session revérifié avec `tsc -b --force` après coup : clean.
- Nouvelle section "Class Avatars" sur `Foundations/Visuals` : 4 avatars de classe (`src/assets/avatars/Class1.svg`–`Class4.svg`, fournis par Marjorie) affichés entre "Avatars" et "Icon Illustrations", même grille/cellule 96px que la section Avatars existante.
- Nouvelle illustration "Time" dans `Foundations/Visuals` → "Icon Illustrations" (`src/assets/illustrations/Time.svg`, fournie par Marjorie), même gabarit 136px que les 7 illustrations existantes.
- Vérifié : `tsc -b --force` clean, rendu live confirmé en Storybook (Playwright), `build-storybook` réussi. Violation a11y "Heading order" (Moderate) constatée sur `Foundations/Visuals` — préexistante (structure `PageLayout`/`Section`, h3→h6, commune à toutes les pages Foundations), pas introduite par ces ajouts, pas traitée.
- `RiBookletLine` ajouté au barrel d'icônes (`src/icons/index.ts`) : demandé par `logbook-dashboard` pour l'onglet "Bulletins" de sa nouvelle page `EvaluationsPage` (liste des évaluations d'une classe) — aucune icône déjà réexportée ne correspondait sémantiquement (livret/bulletin).
- `LogbookNavbar` : nouvelle prop optionnelle `classPlaceholder` (défaut `"Classe"`), affichée à la place du libellé de classe quand `selectedClass` vaut `""` (`displayEmpty`/`renderValue` sur le `Select` interne). Changement rétrocompatible — demandé par `logbook-dashboard` pour sa nouvelle page `Home` (accueil listant toutes les classes, avant toute sélection). Nouvelle section de démo dans la story `Logbook/Navbar` ("Sans classe sélectionnée").
- `LogbookNavbar` : padding horizontal du `MuiToolbar` (`theme/components/Toolbar.ts`) passé de 32px fixe à responsive — 16px (`spacing.sm`) en mobile, 40px (`spacing.xl`) en web, seuil `md` (900px) aligné sur celui déjà utilisé partout ailleurs dans la navbar (logo, boutons Feedback/Profil). `Toolbar` n'a qu'un seul consommateur dans le repo (`LogbookNavbar`), donc pas d'effet de bord ailleurs.
- `LogbookNavbar` : hauteur du `Select` des classes alignée sur celle des `LogbookIconButton size="large"` de la navbar (48px = `spacing['6']`), via `sx` scopé à cette instance (pas au thème `MuiSelect`, qui n'a qu'un seul usage `size="small"` dans tout le repo — alignement propre à cette composition, pas une règle générale). Vérifié en Playwright : hauteurs et position verticale identiques (48px, même `y`), texte non tronqué.
- `Button` (`theme/components/Button.ts`) : nouveau variant `{ variant: 'text' }` annulant `paddingInline`/`paddingBlock` (root + tous les crans de taille) — demandé par Marjorie, padding à 0 sur tous les boutons `text` quelle que soit la couleur/taille. Aucun consommateur de `variant="text"` dans le repo à ce jour, donc pas de régression visuelle ailleurs. `minWidth: 64px` natif de MUI non touché (hors périmètre de la demande, signalé à Marjorie) : peut encore laisser un léger espace visible sur un libellé très court.
- `LogbookNavbar` mobile : tous les `LogbookIconButton` (hors `Select` de classe) désormais groupés dans le même conteneur à droite, plutôt que "Partager les accès" isolé à côté du `Select` — demandé par Marjorie ("tous les icons buttons alignés à droite avec un gap de 8px"). Le bouton texte "Partager les accès" reste à côté du `Select` au-dessus de `NAVBAR_DESKTOP_BREAKPOINT` (940px) ; seule sa version icon button rejoint le groupe de droite (Feedback/Notifications/Profil) en dessous du seuil. Gap du groupe de droite rendu responsive (`sx` fonction de thème, même pattern que `showAboveNavbarBreakpoint`/`hideAboveNavbarBreakpoint`) : `spacing.xs` (8px) sous le seuil, `spacing.sm` (16px, valeur historique) au-dessus. Vérifié en Playwright sur 700/900/939/940/941/1024/1440px : gap de 8px exact entre les 4 icon buttons en mobile, aucun débordement, bascule vers les boutons texte à 940px inchangée.

- Nouveau composant `LogbookFooter` (`src/components/LogbookFooter.tsx`, story `Logbook/Footer`) : pied de page présent sur l'ensemble des pages du produit. Desktop : une ligne (réseaux sociaux icône+libellé à gauche, `Divider` vertical, liens légaux + copyright + pays à droite). Sous le seuil `sm`/600px : deux lignes, regroupement différent (le copyright rejoint les réseaux sociaux, pas les liens légaux) — implémenté comme deux blocs distincts togglés par breakpoint (même logique que Button/IconButton de `LogbookNavbar`), pas une seule ligne réordonnée en CSS. Props : `legalLinks` (`{label, onClick}[]`), `copyrightYear`, `instagram`/`facebook` optionnels (`{label, onClick}`, label doublant d'aria-label en mobile où seule l'icône reste). Comme le reste du DS, tout passe par `onClick` (pas de `href`/routing géré ici, à la charge de l'app consommatrice).
  - **Gap non couvert résolu avec Marjorie** : aucune icône de drapeau pays n'existe dans RemixIcon (catalogue vérifié, seulement des icônes "flag" génériques) ni dans `src/assets/`. Marjorie a fourni le SVG de la pastille France, ajouté tel quel dans `src/assets/flags/France.svg` (couleurs propres à cet asset, `#FF966C`/`#70D4FF`, non tokenisées — même logique que les autres assets statiques du repo, ex. `LogoGreen.svg`).
  - **Icônes réseaux sociaux revues sur demande de Marjorie** : le badge carré foncé (icônes outline blanches sur fond `grey.900`) remplacé par les vraies icônes de marque RemixIcon `RiInstagramFill`/`RiFacebookCircleFill`, en `color.primary.main` (teal), sans fond ni contour — plus besoin de `LogbookIconButton`/badge custom, l'icône est directement colorée via la prop `color` native des composants RemixIcon (même pattern que `LogbookDropzone`/`LogbookCommentField`). Nouvelle exception explicite "outline only" documentée dans `src/icons/index.ts` (même logique que `RiPlayFill`/`RiMoreFill`) : ces deux pictos de marque sont attendus pleins, jamais en contour.
  - Icônes ajoutées au barrel (`src/icons/index.ts`) : `RiInstagramFill`, `RiFacebookCircleFill`, `RiCopyrightLine`.
  - **Bascule mobile/desktop revue sur demande de Marjorie** : un seuil de breakpoint fixe (comme `NAVBAR_DESKTOP_BREAKPOINT` de `LogbookNavbar`) ne convient pas ici — contrairement à la navbar, les libellés (`legalLinks`, `instagram`, `facebook`) sont fournis par l'app consommatrice, donc de longueur imprévisible : un seuil figé aurait fini par laisser un libellé retomber sur 2 lignes dans la ligne desktop pour un jeu de libellés plus long que celui testé en session (repéré par Marjorie : les intitulés de liens ne doivent jamais passer sur 2 lignes, sinon bascule mobile). Remplacé par une mesure réelle (`useFitsOnOneLine`, hook interne à `LogbookFooter.tsx`) : une sonde hors-écran (`position: fixed`, `visibility: hidden`, exclue de l'arbre d'accessibilité) rend le même contenu que la ligne desktop pour connaître sa largeur naturelle, comparée en continu (`ResizeObserver` sur le footer) à la largeur réellement disponible ; bascule vers la version mobile dès que ça ne tiendrait plus sur une ligne, quel que soit le jeu de libellés fourni. Vérifié en Playwright : aucun état intermédiaire à 2 lignes sur toute la plage 320–1440px, bascule propre et réversible dans les deux sens (rétrécir/agrandir), 0 erreur console, 0 violation axe-core (wcag2a/2aa).
  - Vérifié : `tsc -b --force` clean, rendu live en Playwright (desktop conforme à la maquette ; mobile testé à 320–1440px), axe-core (0 violation wcag2a/2aa), `build-storybook` réussi.
  - Gap entre les liens légaux (CGU/Mentions Légales/Confidentialité) en mobile : `spacing.sm` (16px, spec Figma de Marjorie), au lieu de `spacing.xs` (8px) choisi initialement pour gagner de la place à l'affichage. Vérifié en Playwright (16px exact entre les 3 liens à 390px).
  - **Specs Figma desktop en cours de rapprochement avec Marjorie** : padding (16px vertical/`spacing.sm`, 40px horizontal/`spacing.xl`), bordure haute 1px sur `divider`, `justify-content: space-between` déjà conformes. Écart non résolu : hauteur "Hug" attendue à 74px (contenu ≈ 42px hors padding) contre ~53px mesuré (contenu ≈ 21px) — mes liens/icônes actuels (texte brut + `ButtonBase` sans padding) sont ~2× plus courts que prévu. En attente des specs Figma des sous-éléments (lien légal, bouton réseau social) pour trancher plutôt que de deviner un padding.
  - **Tous les liens/mentions du footer passés au style Figma "Button/Label"** (`variant="button"` au lieu de `subtitle2`) : liens légaux, libellés Instagram/Facebook, copyright, "France". A fait remonter un vrai défaut du thème : `typographyOptions.button` (`src/theme/typography.ts`) ne lisait pas le token composite `typography.button.medium` (`tokens/semantic.json`, câblé ailleurs sur `Button.ts`/`Chip.ts` mais jamais sur la variante `Typography` elle-même) — `fontWeight` retombait sur le défaut MUI (500, medium) au lieu des 700 (bold) du token ; `fontSize` (14px) coïncidait par hasard avec le défaut MUI. Corrigé : `fontSize`/`fontWeight`/`letterSpacing` désormais lus depuis `typography.button.medium` (le seul des 3 crans exposable ici, `Typography` n'a pas de prop taille contrairement à `Button`). Aucun consommateur existant de `variant="button"` dans le repo avant cette session (vérifié par recherche) : correction sans régression sur le reste du DS. Vérifié en Playwright (14px/700/lettrage normal desktop et mobile) + axe-core (0 violation wcag2a/2aa), `tsc -b --force` clean, `build-storybook` réussi.
- **`LogbookNavbar` : "+ Ajouter" en bas du Select classe** — nouvelle prop optionnelle `onAddClass`. Quand elle est fournie, un `MenuItem` dédié (séparé par un `Divider`, icône `RiAddLine`, `color: primary.main`) apparait sous la liste des classes ; sa valeur est une sentinelle (`ADD_CLASS_VALUE`) interceptée dans le `onChange` du `Select` pour déclencher `onAddClass` plutôt que `onClassChange` — le `Select` reste contrôlé par `selectedClass` depuis le parent, cette valeur n'est donc jamais reflétée à l'affichage. Reprend et finalise le prototype précédemment annulé (cf. `PROGRESS.md` de `logbook-dashboard`). Nouvelle section de story démontrant cet état. Vérifié : `tsc -b --force` clean.
- **`CorrectionsTableRow` : nouveau champ optionnel `groupId?: string`** (ex. regrouper une fratrie). Le tri alphabétique existant (`order` asc/desc) reste inchangé ; une passe de clustering (`clusterByGroup`, fonction pure au niveau module) s'applique ensuite sur le résultat trié : les lignes qui partagent un `groupId` sont rassemblées à la position du premier membre du groupe rencontré dans l'ordre courant, en conservant leur ordre alphabétique relatif entre elles — les lignes sans `groupId` ne bougent pas. Rendu :
  - Pas de bordure basse entre les lignes d'un même groupe (`isLastOfGroup`, calculé en comparant chaque ligne à la suivante dans le tableau trié+clusterisé) ; seule la dernière ligne du groupe garde la bordure normale portée par `MuiTableCell.root`.
  - Survol synchronisé : `hoveredGroupId` (état du tableau, pas de la ligne) mis à jour via `onMouseEnter`/`onMouseLeave` sur les lignes groupées uniquement (lignes sans `groupId` : comportement `hover` natif MUI inchangé) ; toutes les lignes du groupe survolé reçoivent `theme.palette.action.hover` en fond, pas seulement celle sous le curseur (`:hover` CSS ne peut cibler que l'élément physiquement survolé).
  - Démo ajoutée à `Logbook/CorrectionsTable.stories.tsx` : Amir Haddad/Sofia Ben Ali en fratrie (`groupId: 'fratrie-haddad'`) — délibérément non adjacents alphabétiquement ("Amir" avant "Camille"/"Inès"/"Lucas"/"Nora", "Sofia" après) pour démontrer visiblement le clustering.
  - Vérifié en Playwright : ordre rendu `Amir, Sofia, Camille, Inès, Lucas, Nora` (Sofia bien ramenée juste après Amir) ; bordure absente entre Amir/Sofia, normale partout ailleurs ; survol de Sofia met en fond `action.hover` sur Amir ET Sofia simultanément, aucun effet sur Camille (ligne de contrôle hors groupe) ; 0 violation axe-core (wcag2a/2aa). `tsc -b --force` clean, `build-storybook` réussi.
- **`LogbookSidePanelHeader` : support d'un devoir de groupe** (demandé par Marjorie, pour le SidePanel de correction d'un devoir fait à plusieurs). API changée de `studentName`/`avatarSrc`/`avatarAlt` vers `students: {name, avatarSrc?, avatarAlt?}[]` (seuls consommateurs : ce repo et `logbook-dashboard`, tous deux mis à jour) : un seul élève dans le tableau → rendu strictement identique à avant (avatar 48px + nom) ; plusieurs → avatars 40px légèrement superposés (`margin-left: -12px`, liseré 2px `paper-elevation-0` pour les séparer visuellement) suivis des noms sur 1 à 2 lignes (`WebkitLineClamp: 2`). Nouvelle section de story "Devoir de groupe" démontrant le cas à 3 élèves. `RiScalesLine` ajouté au barrel d'icônes (bloc "Bonus/malus" du SidePanel côté `logbook-dashboard`). Vérifié : `tsc -b --force` clean, `build-storybook` réussi.
- **2 corrections après retour de Marjorie sur le SidePanel de correction, comparé au vrai Figma du design system** (`efyvSZ3kjbDu97ZtJ6Mgmu`, node `252:8955` — jusque-là seule une maquette isolée avait été fournie, sans accès au fichier source de vérité) :
  - **`LogbookSidePanelHeader` avatars de groupe refaits avec le vrai `<AvatarGroup>` MUI** (au lieu du liseré+margin négative fait main de l'entrée précédente) — le composant `<AvatarGroup>` était explicitement documenté sur le node Figma (252:9126) comme référence à utiliser. Avatars ramenés à 32px (au lieu de 40px, ne correspondaient pas à la taille réelle mesurée sur la maquette), `spacing={16}` pour un chevauchement proche de la maquette (~30px de large par avatar visible sur un groupe de 3, mesuré sur le node).
  - **`MuiOutlinedInput` (`TextField.ts`) : `borderRadius` corrigé de `borderRadius.sm` (4px) à `borderRadius.md` (8px)** — tous les inputs de la maquette Figma du SidePanel (Points positifs, grille de notation, ressource, "Partagé avec les élèves"...) affichent systématiquement 8px, jamais 4px. Change le rayon de TOUS les `TextField` de l'app (pas seulement le SidePanel) : vérifié sur la story `TextField` (aucune régression visuelle, rayon simplement plus prononcé, cohérent avec le reste des composants déjà en `borderRadius.md`, ex. `Input` des chips "Partagé avec les élèves").
  - Vérifié : `tsc -b --force` clean, `oxlint` clean, stories `SidePanelHeader`/`TextField` revérifiées en Playwright.

## Dette technique

- **Tree-shaking du barrel `src/index.ts`** : le build de `logbook-dashboard` (qui n'importe que `LogbookStatCard`) embarque quand même `LogoGreen.svg`, un asset utilisé uniquement par `LogbookNavbar` — le barrel réexporte tous les composants Logbook depuis un seul fichier, et sans `"sideEffects": false` dans `package.json`, Rollup ne peut pas prouver que les imports d'assets inutilisés sont sans effet de bord, donc les garde. À corriger : ajouter `"sideEffects": false` (ou la liste précise des fichiers avec effets de bord, ex. imports CSS globaux s'il y en a un jour) dans `package.json`. Affecte tous les projets consommateurs du design system, pas seulement `logbook-dashboard`.

## En cours / à moitié fait
- Vue "correction déjà faite" du side panel élève (`logbook-dashboard`) : les 3 champs de feedback de la maquette sont désormais couverts et intégrés côté dashboard (`LogbookCommentField` pour "Points positifs"/"Conseils pour progresser", `LogbookDropzone` pour la zone de dépôt), sans la bordure colorée par champ envisagée initialement sur la maquette (contour beige standard, simplification assumée pour l'instant). Le header icône+libellé de "Ressources complémentaire" et son champ lien restent composés directement dans `App.tsx` (icône + `Typography`/`TextField` MUI standard) — aucun composant DS dédié pour ce sous-ensemble précis, pas identifié comme nécessaire à ce stade.
- **A11y du clic de ligne sur `CorrectionsTable`** : ouvre le side panel uniquement à la souris — le `TableRow` n'est pas focusable/activable au clavier (pas de `tabIndex`, pas de gestion `Enter`/`Espace`). Signalé à Marjorie, pas encore traité ; demande un pattern dédié de ligne de tableau interactive si l'accès clavier au side panel est requis pour l'AA.
- `Badge` et `Menu` : utilisables via `@mui/material` (héritent du thème global palette/typo) mais sans `styleOverrides` Logbook dédié dans `src/theme/components/` — pas encore habillés. Besoin identifié côté `logbook-dashboard` : `Badge` pour un compteur de notifications, `Menu` pour les actions "..." de chaque ligne de `CorrectionsTable`. À thémer à la demande, priorité proche.
- Aucun contournement appliqué pour l'alerte axe-core `aria-conditional-attr` déclenchée par `Checkbox indeterminate` (comportement délibéré de MUI, pas un défaut du thème — documenté dans SKILL.md, pas traité).
- Runner de tests automatisés (`npx vitest run`) cassé : incompatibilité CJS/ESM en cascade (`aria-query`, `lz-string`, `pretty-format`...) sur la pile Storybook 10 + Vite 8 + `@storybook/addon-vitest` — bug d'écosystème upstream, pas de fix côté projet à ce stade.

## Décisions en attente

- **Version MUI** : v9 utilisée actuellement, non tranchée — à valider avec l'équipe dev Logbook avant toute nouvelle génération de composants.
- **Catégorisation Storybook de `Tabs`** : rangé sous `Components/Tabs` (composant MUI thémé, même logique que Button/Chip/Card) plutôt que sous `Logbook/`, alors que la demande initiale mentionnait "pour Logbook" — à confirmer avec Marjorie.
- **Contour `outlined secondary` (Button.ts)** : exception déjà assumée (contraste 1.30:1 contre blanc, sous le seuil de 3:1) désormais aussi visible sur fond beige de header de table (encore moins contrasté) depuis les essais sur le bouton "Élève" de `CorrectionsTable` — à revisiter si l'usage se généralise.

## Prochaine étape

1. Brancher Figma via le plugin Tokens Studio depuis le repo GitHub (le repo est déjà poussé, condition remplie) — process détaillé dans `.claude/skills/logbook-design-system/SKILL.md`, section "Synchroniser avec Figma".
2. Générer la bibliothèque de composants Figma correspondante depuis Tokens Studio.
